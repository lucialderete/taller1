# TP1 - EJERCICIO 4
**4C:** aparecen los archivos _README.md_, _main.c_ y _main.exe_.  el archivo .exe que no es necesario que se envie al repositorio


**4G:** el apartado 2 muestra la _direccion de memoria almacenada por el puntero_ mientras que el apartado 3  la _direccion de momemoria de la variable_, dado que el puntero fue declarado para apuntar a esa variable, entonces almacena la direccion de memoria y por eso muestra exactamente el mismo valor.

el apartado 4 obtiene la _direccion de memoria del propio puntero_ y no es igual al valor de los aparatados anteriores porque el puntero tambien es una variable independiente por lo que tiene su propio espacio en la memoria